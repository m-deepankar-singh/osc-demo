# API Keys
FIRECRAWL_API_KEY=fc-your_key_here  # Must start with fc-
FIRECRAWL_BASE_URL=  # Optional, only if using self-hosted instance

# Other Configuration
NODE_ENV=development

# Note: Never commit your actual API keys to version control
# Copy this file to .env and replace the placeholder values with your actual keys
# For Firecrawl API key, ensure it starts with 'fc-' 